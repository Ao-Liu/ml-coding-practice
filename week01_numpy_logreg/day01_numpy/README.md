# Day 1: NumPy basics → simple broadcasting

Open `exercises.py`: all explanations, examples, and tasks are next to the functions. Your completed exercises and personal notes are preserved.

Work in order: 1–5 review, 6–9 slicing and masks, 10–13 aggregation and reshape, 14–15 broadcasting.

From the repository root, with your environment activated:

```bash
# Replace first_rows with the function you are practicing:
python -m pytest -q week01_numpy_logreg/day01_numpy -k first_rows
# Run all exercises:
python -m pytest -q week01_numpy_logreg/day01_numpy
```

Unfinished exercises are expected to fail with `NotImplementedError`.
